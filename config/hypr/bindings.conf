# Application bindings
bindd = SUPER, RETURN, Terminal, exec, uwsm-app -- xdg-terminal-exec --dir="$(omadora-cmd-terminal-cwd)"
bindd = SUPER, E, File Explorer, exec, uwsm-app -- nautilus --new-window
bindd = SUPER, B, Browser, exec, omadora-launch-browser
bindd = SUPER SHIFT, B, Browser (private), exec, omadora-launch-browser --private
bindd = SUPER , N, Editor, exec, omadora-launch-editor

# If your web app url contains #, type it as ## to prevent hyprland treating it as a comment
bindd = SUPER , M, Google Maps, exec, omadora-launch-webapp "https://maps.google.com/"
bindd = SUPER , A, ChatGPT, exec, omadora-launch-webapp "https://chatgpt.com"
bindd = SUPER , Y, YouTube, exec, omadora-launch-webapp "https://youtube.com/"

# Overwrite existing bindings, like putting Omadora Menu on Super + Space
# unbind = SUPER, SPACE
# bindd = SUPER, SPACE, Omadora menu, exec, omadora-menu

# Unbind and remap defaults for vim keys
unbind = SUPER, J
unbind = SUPER, K
bindd = SUPER CTRL, K, Show key bindings, exec, omadora-menu-keybindings
unbind = SUPER, W
bindd = SUPER, W, Close window, killactive,
unbind = SUPER, T
bindd = SUPER, J, Toggle window split, togglesplit, # dwindle
unbind = SUPER, F
bindd = SUPER, T, Toggle window floating/tiling, togglefloating,
unbind = SUPER SHIFT, F
bindd = SUPER, F, Full screen, fullscreen, 0
bindd = SUPER ALT, F, Full screen, fullscreen, 1
bindd = SUPER SHIFT, F, exec, fullscreenstate, -1 2
bindd = SUPER, H, Move window focus left, movefocus, l
bindd = SUPER, L, Move window focus right, movefocus, r
bindd = SUPER, K, Move window focus up, movefocus, u
bindd = SUPER, J, Move window focus down, movefocus, d
bindd = SUPER SHIFT, H, Swap window to the left, swapwindow, l
bindd = SUPER SHIFT, L, Swap window to the right, swapwindow, r
bindd = SUPER SHIFT, K, Swap window up, swapwindow, u
bindd = SUPER SHIFT, J, Swap window down, swapwindow, d
